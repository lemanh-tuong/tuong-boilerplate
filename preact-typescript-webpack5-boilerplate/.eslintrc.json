{
  "extends": ["../../.eslintrc.json"],
  "ignorePatterns": ["!**/*"],
  "settings": {
    "pragma": "h"
  },
  "rules": {
    "@typescript-eslint/no-unused-vars": [
      "error",
      {
        "varsIgnorePattern": "h" // Ignore h being imported and not used explicitly
      }
    ]
  }
}
